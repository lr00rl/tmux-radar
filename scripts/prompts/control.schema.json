{
  "type": "object",
  "additionalProperties": false,
  "required": ["commands", "explain"],
  "properties": {
    "commands": {
      "type": "array",
      "items": { "type": "string" },
      "description": "Ordered layout-only tmux argv lines without the leading 'tmux' word. No quoting, shell commands, or executable formats."
    },
    "explain": {
      "type": "string",
      "description": "One or two sentences in the user's language describing the layout change or why the request is outside this layout-only surface."
    }
  }
}
