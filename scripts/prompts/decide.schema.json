{
  "type": "object",
  "additionalProperties": false,
  "required": ["action", "text", "keys", "safe", "reason"],
  "properties": {
    "action": {
      "type": "string",
      "enum": ["send", "wait", "done", "escalate", "suggest"],
      "description": "send=answer a blocked prompt; wait=busy/not blocked; done=configured goal achieved; escalate=hand back to human; suggest=recommend without acting"
    },
    "text": {
      "type": "string",
      "description": "Literal characters to type (may be empty or contain spaces). Sent before keys."
    },
    "keys": {
      "type": "array",
      "items": { "type": "string" },
      "description": "tmux key names sent after text, in order (e.g. [\"Enter\"], [\"Down\",\"Enter\"], [\"1\"])."
    },
    "safe": {
      "type": "boolean",
      "description": "false for destructive/irreversible/ambiguous actions — forces escalation."
    },
    "reason": {
      "type": "string",
      "description": "One concise sentence explaining the pane state and the chosen action."
    },
    "pane_state": {
      "type": "string",
      "enum": ["working", "blocked", "idle", "done", "unknown"],
      "description": "Optional observable state of the target pane."
    },
    "goal_status": {
      "type": "string",
      "enum": ["working", "blocked", "done", "unclear"],
      "description": "Optional progress of the exact configured goal."
    },
    "risk": {
      "type": "string",
      "enum": ["low", "medium", "high", "unknown"],
      "description": "Optional risk level of the proposed action."
    },
    "evidence": {
      "type": "array",
      "items": { "type": "string" },
      "description": "Optional short observable facts from the pane supporting the decision."
    }
  }
}
